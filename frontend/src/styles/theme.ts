import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'blue',
  fontFamily: 'var(--font-geist-sans), Arial, sans-serif',
  headings: { fontFamily: 'var(--font-geist-sans), Arial, sans-serif' },
});
